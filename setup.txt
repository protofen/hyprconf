sudo pacman -Syu
sudo pacman -S code waybar hyprlauncher git flatpak libreoffice-still wine wine-mono wine-gecko python python-pip hyprpaper mpv nwg-look nwg-displays fastfetch brightnessctl pipewire pipewire-pulse pipewire-alsa pipewire-jack wireplumber sof-firmware alsa-ucm-conf alsa-firmware alsa-utils shutter
sudo pacman -S --needed base-devel git
git clone https://aur.archlinux.org/yay.git
cd yay
makepkg -si
cd ..
rm -rf yay
yay
yay -S google-chrome
python -m venv .venv
hyprctl monitors
